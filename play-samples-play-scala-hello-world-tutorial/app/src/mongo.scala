package src

import factories.MongoFactory
import model.User

object mongo {
    def main(args: Array[String]): Unit = {
//      val user:User = new User("ullayne", 10)
//      val mongo = new MongoFactory("test")
//      mongo.insertUser(user)
//      var result = mongo.getUserByName("ullayne")
//      println(result)
    }
}